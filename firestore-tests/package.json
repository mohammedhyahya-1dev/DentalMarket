{
  "name": "dentalmarket-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules regression tests run against the Firestore emulator — same regression-suite role as the Kotlin OrderViewModelTest/ContactInfoFilterTest, but for firestore.rules itself, which those can't exercise directly.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2"
  }
}
